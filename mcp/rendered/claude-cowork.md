# Claude Cowork — MCP connectors

Generated by `scripts/render-mcp-configs.py` from `mcp/servers.yaml`.
Do not edit this file manually — your changes will be overwritten.

Client: **claude-cowork** — Claude running inside the Cowork desktop app.

Cowork custom connectors are added through the UI
(Settings → Custom MCP Connectors → Add). For each server below,
create an entry with the listed URL and an `Authorization: Bearer …`
header using the referenced secret.

---

## n8n-mcp

Community n8n-mcp server (rich builder layer on top of the n8n API).

- **URL**: `https://n8n-mcp.samandrey.work/mcp`
- **Transport**: `http`
- **Header**: `Authorization: Bearer ${N8N_MCP_AUTH_TOKEN}` (value from `.env` variable `N8N_MCP_AUTH_TOKEN` on the platform VM)

**Notes:**

> Image pinned by digest in docker-compose.yml (commit ba43ec9).
> See docs/N8N-MCP.md for deployment and smoke-test procedure.
