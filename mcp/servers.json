{
  "_comment": [
    "Single source of truth for MCP server configuration.",
    "Edit this file, then run: python3 scripts/render-mcp-configs.py",
    "Commit the manifest edit and regenerated mcp/rendered/ in one commit.",
    "Rendered files contain placeholders like ${N8N_MCP_AUTH_TOKEN}, never real tokens.",
    "Format is plain JSON so the renderer has zero external dependencies — reproducible from a clean clone on any Python 3.x."
  ],
  "servers": {
    "n8n-mcp": {
      "description": "Community n8n-mcp server (rich builder layer on top of the n8n API).",
      "url": "https://n8n-mcp.samandrey.work/mcp",
      "transport": "http",
      "auth": {
        "type": "bearer",
        "token_env": "N8N_MCP_AUTH_TOKEN"
      },
      "enabled_for": ["claude-cowork", "codex"],
      "notes": "Image pinned by digest in docker-compose.yml (commit ba43ec9). Codex uses direct remote HTTP MCP config; avoid wrapping this endpoint in mcp-remote for Codex because that stdio bridge can turn server-side protocol errors into 120s tool-call timeouts. See docs/RUNBOOK.md (## n8n-mcp) for deployment and smoke-test procedure."
    }
  },
  "clients": {
    "claude-cowork": {
      "kind": "cowork-desktop-config",
      "description": "Claude running inside the Cowork desktop app.",
      "config_path": "%LOCALAPPDATA%\\Packages\\Claude_pzs8sxrjxfjjc\\LocalCache\\Roaming\\Claude\\claude_desktop_config.json",
      "config_hint": "Merge the rendered mcpServers block into claude_desktop_config.json inside the MSIX sandbox. Cowork bridges host-side MCP servers into its VM as type='sdk'."
    },
    "codex": {
      "kind": "codex-cli",
      "description": "OpenAI Codex Desktop / CLI.",
      "config_path": "~/.codex/config.toml",
      "config_hint": "Paste the rendered block into the user config file and fully restart Codex; existing MCP client sessions do not reread config."
    }
  }
}
