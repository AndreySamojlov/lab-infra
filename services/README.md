# Services Overlay

`services/` is reserved for optional local-only overlays.

The committed platform repo must not contain business-specific service
implementations here. If you need a private helper service for one
project, keep it untracked locally or move it into that project's own
repository.
