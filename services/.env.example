# Reserved for optional project-specific service overlays.
# This platform repo intentionally ships no business/project services.
# If you temporarily add one locally, keep its variables in a private
# `services/.env` and do not commit the service implementation back here.

# Shared Docker network where n8n can resolve optional internal service aliases.
LAB_SERVICES_NETWORK=lab-internal
